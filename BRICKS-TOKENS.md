## Bricks Design System reference — `<MCP_SERVER_NAME>`

> **PLACEHOLDER — generated per site.** This file holds the site's full `brxw-*` / `brxp-*` variable + global-class inventory (names only, `*-fluid` excluded). Run the **`bricks-design-tokens`** skill against the site's connected Novamira MCP server to populate it. Until then, use the naming-patterns cheat-sheet in `CLAUDE.md` and treat the live Bricks Style Manager as the source of truth.
